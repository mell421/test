begin_time <-Sys.time()
suppressWarnings(source("./R/sources.R"))
# by(data, data$category, summary)
# Hmisc::describe(data)
print("accueil")
accue <- data.frame(accueil())
begin_time <-Sys.time()
suppressWarnings(source("./R/sources.R"))
begin_time <-Sys.time()
suppressWarnings(source("./R/sources.R"))
library(vtable)
summar <- function(i){
print(i)
print(z0n(i))
tab <- st(z0u(i))
tab
}
summar(1)
# by(data, data$category, summary)
# Hmisc::describe(data)
accue <- data.frame(accueil())
# by(data, data$category, summary)
# Hmisc::describe(data)
#a2 <- (st(data))
#a2
accue <- data.frame(accueil())
## SUMMARY
### stats
#by(data, data$category, summary)
#Hmisc::describe(data)
#a2 <- (st(data))
#a2
accue <- data.frame(accueil())
